<!-- Generated by scripts/docs/generate_model_verification_catalog.py. Do not edit. -->

# moonshotai/Kimi-K3

Performance disclaimer: this model has not been performance-tuned; reported timing and throughput metrics are sanity checks, not optimized performance results. This card covers the Kimi K3 language backbone in the published multimodal checkpoint. It does not claim support for the vision tower or multimodal inputs. Full-checkpoint GPU import and deterministic Megatron greedy text generation are verified. CPU conversion, GPU export, full-model Hugging Face/Megatron forward correlation, and every training workflow remain unverified.


## Provenance

| Field | Value |
| --- | --- |
| Card | [`examples/model_verification_cards/kimi-k3/card.yaml`](https://github.com/NVIDIA-NeMo/Megatron-Bridge/blob/main/examples/model_verification_cards/kimi-k3/card.yaml) |
| Hugging Face model | [`moonshotai/Kimi-K3`](https://huggingface.co/moonshotai/Kimi-K3) |
| Hugging Face revision | `9f62e4e9fffbd0a83ddd60e1c209d828994b3569` |
| Architecture | `KimiK3ForConditionalGeneration` |
| Minimum Transformers | `4.56.2` |
| Base container | `nvcr.io/nvidia/nemo:26.06` |
| Card-level Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |
| Complementary model documentation | [Open model guide](../../models/kimi/kimi-k3) |
| Aggregate state | **partially verified** |

Concrete status counts: ✅ verified: **2**, ⚪ unverified: **10**, ❌ unsupported: **0**, ➖ not applicable: **0**.

## Concrete configurations

Each section below is one existing card leaf. Hardware and precision are shown only when the card states them; missing values are not inferred from prose or neighboring entries.

<a id="kimi-k3-hf-to-megatron-cpu"></a>
### hf_to_megatron_cpu

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.hf_to_megatron_cpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

No CPU Hugging Face-to-Megatron conversion result is claimed. Future verification must import the complete pinned language backbone with the public conversion launcher, persist iter_0000000, reload it, and audit all mapped keys, shapes, dtypes, and values.

<a id="kimi-k3-hf-to-megatron-gpu"></a>
### hf_to_megatron_gpu

| Field | Value |
| --- | --- |
| Status | **✅ verified** |
| Source pointer | `items.hf_to_megatron_gpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `2026-07-28` |
| Effective Bridge commit | `cec012e568c55a6c0906622d8a5b0986a79f40cf` |

#### Explicit command topology

| Command | Explicit topology |
| --- | --- |
| 1 | `nodes=12, gpus_per_node=4, tensor_parallel=2, pipeline_parallel=3, expert_parallel=8, expert_tensor_parallel=2, total_gpus=48` |

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

```bash
./scripts/conversion/convert.sh import --executor slurm --device gpu --nodes 12 --gpus-per-node 4 --hf-model moonshotai/Kimi-K3 --hf-revision 9f62e4e9fffbd0a83ddd60e1c209d828994b3569 --megatron-path work/model-verification/kimi-k3/imported-megatron --torch-dtype bfloat16 --tp 2 --pp 3 --ep 8 --etp 2 --distributed-timeout-minutes 180 --trust-remote-code
```

#### Expected result

On 48x GB200, the pinned complete checkpoint imports at TP2/PP3/EP8/ETP2 and exits successfully in 27:01. Published MXFP4 routed-expert weights are dequantized to BF16, and iter_0000000 is persisted with 48 distributed-checkpoint shards, metadata, and the recorded 93-layer run configuration. Exact Hugging Face round-trip parity is tracked separately by megatron_to_hf_gpu.

<a id="kimi-k3-megatron-to-hf-cpu"></a>
### megatron_to_hf_cpu

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.megatron_to_hf_cpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

No CPU Megatron-to-Hugging-Face conversion result is claimed. Future verification depends on a complete persisted Megatron import and must write, strictly reload, and exactly audit the exported Hugging Face language-backbone weights against the pinned source revision.

<a id="kimi-k3-megatron-to-hf-gpu"></a>
### megatron_to_hf_gpu

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.megatron_to_hf_gpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

No GPU Megatron-to-Hugging-Face conversion result is claimed. All 69 source KDA A_log tensors were audited: each stores 96 active heads plus 32 zero-valued inactive entries, for 2,208 checked padding values. Import validation drops only this zero padding, and export unit tests restore it with zeros. A four-layer diagnostic proxy completed low-memory GPU import and distributed streaming export. Its seven safetensors shards and index, totaling 58,482,941,904 bytes, were byte-identical to an export from the previous default-save import; this validates the low-memory I/O behavior, not full-model parity. Against the published proxy source, all 16,402 keys and shapes matched and A_log padding was valid. Three o_norm tensors changed from FP32 to BF16, and 16,128 value mismatches were concentrated in MXFP4 expert packed weights and scales because import dequantizes them to BF16 before export requantizes them. Full verification therefore still requires the pinned complete model to export and strictly reload, exact comparison of lossless tensors, and a declared numerical criterion for lossy MXFP4 reconstruction.

<a id="kimi-k3-manual-forward-pass"></a>
### manual_forward_pass

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.manual_forward_pass` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A four-layer diagnostic proxy matched the next token and measured logit cosine similarity 0.9998329878 and Pearson correlation 0.9998273849. This is not a full-model comparison. Verification still requires the pinned complete language backbone on both implementations, the same prompt, a matching next token, cosine similarity of at least 0.99, and reported maximum and mean absolute logit differences.

<a id="kimi-k3-inference"></a>
### inference

| Field | Value |
| --- | --- |
| Status | **✅ verified** |
| Source pointer | `items.inference` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `2026-07-27` |
| Effective Bridge commit | `eb91f8be86e5f88771bfa020b33cd74dd3de80ad` |

#### Explicit command topology

| Command | Explicit topology |
| --- | --- |
| 1 | `tensor_parallel=1, pipeline_parallel=3, expert_parallel=8, expert_tensor_parallel=1` |

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

```bash
uv run python examples/conversion/hf_to_megatron_generate_text.py --hf_model_path moonshotai/Kimi-K3 --trust-remote-code --apply-chat-template --thinking-mode disabled --prompt "What is 15% of 240? Answer in one short sentence." --max_new_tokens 32 --tp 1 --pp 3 --ep 8 --etp 1
```

#### Expected result

On 24x GB300, the pinned complete language backbone imports directly into Megatron at TP1/PP3/EP8/ETP1. The command exits successfully in 22:59 after exactly 32 new greedy tokens and prints this completion, represented with JSON Unicode escapes for the model control-token brackets: "15% of 240 = 0.15 × 240 = 36.\u003c|close|\u003ethink\u003c|sep|\u003e\u003c|open|\u003eresponse\u003c|sep|\u003e15% of 240 is 36.".

<a id="kimi-k3-pretrain-gb300"></a>
### pretrain · GB300

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.pretrain.GB300` |
| Hardware | `GB300` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

Training was intentionally excluded from this model-only verification pass. A future public Kimi K3 language-backbone recipe must complete a bounded 100-step pretrain run with finite loss, no skipped or NaN iterations, all five metrics, and a reloadable final checkpoint.

<a id="kimi-k3-sft-gb300"></a>
### sft · GB300

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.sft.GB300` |
| Hardware | `GB300` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

Training was intentionally excluded from this model-only verification pass. A future public Kimi K3 language-backbone recipe must complete a pinned-data 100-step full-SFT run with finite loss, no skipped or NaN iterations, all five metrics, and a reloadable final checkpoint.

<a id="kimi-k3-sft-export-inference-gb300"></a>
### sft_export_inference · GB300

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.sft_export_inference.GB300` |
| Hardware | `GB300` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

No post-SFT export or inference result is claimed because SFT was intentionally excluded from this model-only verification pass. Future verification requires a completed SFT run followed by a strict Hugging Face export reload and deterministic greedy text generation.

<a id="kimi-k3-sft-long-context-gb300"></a>
### sft_long_context · GB300

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.sft_long_context.GB300` |
| Hardware | `GB300` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

Training was intentionally excluded from this model-only verification pass. A future public long-context SFT recipe must define supported sequence packing and context parallelism, complete 100 steps with finite loss and no skipped or NaN iterations, and record all five metrics.

<a id="kimi-k3-peft-gb300"></a>
### peft · GB300

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.peft.GB300` |
| Hardware | `GB300` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

Training was intentionally excluded from this model-only verification pass. A future Kimi K3 PEFT recipe with an audited adapter target set must complete 100 steps with finite loss, no skipped or NaN iterations, all five metrics, and a reloadable adapter checkpoint.

<a id="kimi-k3-checkpoint-resume-gb300"></a>
### checkpoint_resume · GB300

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.checkpoint_resume.GB300` |
| Hardware | `GB300` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `ae77a6703c61669f15936a12ddf542c2cf5cbc3b` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

Training was intentionally excluded from this model-only verification pass. A future direct continuation must restore model, optimizer, scheduler, data-order, and RNG state, match declared sentinel losses within tolerance, and save a reloadable checkpoint to a distinct output root.
