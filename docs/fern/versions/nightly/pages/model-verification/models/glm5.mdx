<!-- Generated by scripts/docs/generate_model_verification_catalog.py. Do not edit. -->

# zai-org/GLM-5

Performance disclaimer: this model has not been performance-tuned; reported timing and throughput metrics are sanity checks, not optimized performance results. Model-level verification covers deterministic HybridEP inference and exact distributed GPU import/export of the pinned GLM-5 Hugging Face checkpoint's 78-layer inference graph. The checkpoint's appended MTP auxiliary layer is intentionally disabled and excluded from those claims. CPU conversion, Hugging Face/Megatron forward parity, training, checkpoint resume, and post-SFT export/reload verification are deferred.


## Provenance

| Field | Value |
| --- | --- |
| Card | [`examples/model_verification_cards/glm5/card.yaml`](https://github.com/NVIDIA-NeMo/Megatron-Bridge/blob/main/examples/model_verification_cards/glm5/card.yaml) |
| Hugging Face model | [`zai-org/GLM-5`](https://huggingface.co/zai-org/GLM-5) |
| Hugging Face revision | `4e6698ba8e85059d749020e3c4d2123719f23926` |
| Architecture | `GlmMoeDsaForCausalLM` |
| Minimum Transformers | `5.12.1` |
| Base container | `nvcr.io/nvidia/nemo:26.08.rc3` |
| Card-level Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |
| Complementary model documentation | [Open model guide](../../models/glm/glm5) |
| Aggregate state | **partially verified** |

Concrete status counts: ✅ verified: **3**, ⚪ unverified: **9**, ❌ unsupported: **0**, ➖ not applicable: **0**.

## Concrete configurations

Each section below is one existing card leaf. Hardware and precision are shown only when the card states them; missing values are not inferred from prose or neighboring entries.

<a id="glm5-hf-to-megatron-cpu"></a>
### hf_to_megatron_cpu

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.hf_to_megatron_cpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A CPU import of the pinned Hugging Face revision must complete every mapping and create a reloadable Megatron checkpoint. This workflow is deferred by this card.

<a id="glm5-hf-to-megatron-gpu"></a>
### hf_to_megatron_gpu

| Field | Value |
| --- | --- |
| Status | **✅ verified** |
| Source pointer | `items.hf_to_megatron_gpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `2026-08-10` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

| Command | Explicit topology |
| --- | --- |
| 1 | `nodes=4, gpus_per_node=8, tensor_parallel=1, pipeline_parallel=2, expert_parallel=8, expert_tensor_parallel=2, total_gpus=32` |

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

```bash
./scripts/conversion/convert.sh import --executor slurm --device gpu --nodes 4 --gpus-per-node 8 --hf-model zai-org/GLM-5 --hf-revision 4e6698ba8e85059d749020e3c4d2123719f23926 --megatron-path work/model-verification/glm5/gpu-megatron --torch-dtype bfloat16 --tp 1 --pp 2 --ep 8 --etp 2 --distributed-timeout-minutes 60 --low-memory-save
```

#### Expected result

The pinned 32-H100 import exits successfully at TP1/PP2/EP8/ETP2, completes all 6,201 distributed mapping tasks, and persists a reloadable iter_0000000 checkpoint. Reload plus exact export projection covers all 59,079 tensors and 1,487,822,475,264 tensor-payload bytes in the 78-layer inference graph with matching keys, shapes, dtypes, and values. The 791 source tensors under model.layers.78 belong only to the intentionally disabled appended MTP auxiliary layer and are outside this item.

<a id="glm5-megatron-to-hf-cpu"></a>
### megatron_to_hf_cpu

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.megatron_to_hf_cpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A CPU export from a verified Megatron checkpoint must preserve every mapped tensor and strictly reload in Transformers. This workflow is deferred by this card.

<a id="glm5-megatron-to-hf-gpu"></a>
### megatron_to_hf_gpu

| Field | Value |
| --- | --- |
| Status | **✅ verified** |
| Source pointer | `items.megatron_to_hf_gpu` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `2026-08-10` |
| Effective Bridge commit | `7e2181dcc5d09a1a859dfb0c655c9a1cd486e0ac` |

#### Explicit command topology

| Command | Explicit topology |
| --- | --- |
| 1 | `nodes=4, gpus_per_node=8, tensor_parallel=1, pipeline_parallel=2, expert_parallel=8, expert_tensor_parallel=2, total_gpus=32` |

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

```bash
./scripts/conversion/convert.sh export --executor slurm --device gpu --nodes 4 --gpus-per-node 8 --hf-model zai-org/GLM-5 --hf-revision 4e6698ba8e85059d749020e3c4d2123719f23926 --megatron-path work/model-verification/glm5/gpu-megatron/iter_0000000 --hf-path work/model-verification/glm5/gpu-hf-export --torch-dtype bfloat16 --tp 1 --pp 2 --ep 8 --etp 2 --distributed-timeout-minutes 60 --distributed-save --save-every-n-ranks 1
```

#### Expected result

The 32-H100 distributed export exits successfully after all 6,201 mapping tasks and writes 280 safetensors shards. Of those, 278 are byte-for-byte identical to the pinned source shards; the two shards that also contain excluded MTP keys match all 63 common tensors exactly. The composite audit covers all 59,079 inference-graph tensors and 1,487,822,475,264 tensor-payload bytes with zero key, shape, dtype, or value mismatch. Transformers 5.12.1 strictly reloads the output as GlmMoeDsaForCausalLM with 1,629 state tensors, 743,911,199,232 parameters, and no loading discrepancies.

<a id="glm5-manual-forward-pass"></a>
### manual_forward_pass

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.manual_forward_pass` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A pinned first-token Hugging Face versus Megatron comparison must match the argmax token and meet the card's logit-correlation threshold. The full Hugging Face reference pass is deferred by this card.

<a id="glm5-inference"></a>
### inference

| Field | Value |
| --- | --- |
| Status | **✅ verified** |
| Source pointer | `items.inference` |
| Hardware | `not specified` |
| Precision | `bf16` |
| Last verified | `2026-08-10` |
| Effective Bridge commit | `b58afee863f40b64304373830ae79f3f61a1af19` |

#### Explicit command topology

| Command | Explicit topology |
| --- | --- |
| 1 | `nodes=8, gpus_per_node=8, tensor_parallel=1, pipeline_parallel=2, expert_parallel=32, expert_tensor_parallel=1, total_gpus=64` |

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

```bash
./scripts/inference/infer.sh --nodes 8 --gpus-per-node 8 --task legacy-full-prefix-generation --hf_model_path zai-org/GLM-5 --hf-revision 4e6698ba8e85059d749020e3c4d2123719f23926 --prompt "What is artificial intelligence?" --max_new_tokens 20 --legacy-full-prefix --tp 1 --pp 2 --ep 32 --etp 1
```

#### Expected result

One deterministic BF16 run loads the pinned Hugging Face model directly
into Megatron across 64 H100 ranks at TP1/PP2/EP32/ETP1 and uses the
flex HybridEP dispatcher. The inference graph intentionally excludes the
checkpoint's appended MTP auxiliary layer. It exits successfully after
exactly 20 new greedy tokens. The literal completion, including its
leading space, was:
" How is artificial intelligence going to change our lives?"

<a id="glm5-pretrain-h100"></a>
### pretrain · H100

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.pretrain.H100` |
| Hardware | `H100` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A public GLM-5 H100 recipe must complete a bounded 100-step run with finite loss, no skipped or NaN iterations, all five metrics, and a reloadable final checkpoint. Training is deferred by this card.

<a id="glm5-sft-h100"></a>
### sft · H100

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.sft.H100` |
| Hardware | `H100` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A pinned-data 100-step full-SFT run must finish with finite loss, no skipped or NaN iterations, all five metrics, and a reloadable final checkpoint. Training is deferred by this card.

<a id="glm5-sft-export-inference-h100"></a>
### sft_export_inference · H100

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.sft_export_inference.H100` |
| Hardware | `H100` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

None recorded.

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A verified SFT checkpoint must export to Hugging Face, strictly reload, and produce deterministic checkpoint-backed inference. Training and post-SFT export verification are deferred by this card.

<a id="glm5-sft-long-context-h100"></a>
### sft_long_context · H100

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.sft_long_context.H100` |
| Hardware | `H100` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A dedicated packed long-context SFT run must complete a bounded run with finite loss, no skipped or NaN iterations, all five metrics, and a reloadable checkpoint. Training is deferred by this card.

<a id="glm5-peft-h100"></a>
### peft · H100

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.peft.H100` |
| Hardware | `H100` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A GLM-5 PEFT recipe with an audited adapter target set must complete a bounded run with finite loss, all five metrics, and a reloadable adapter checkpoint. Training is deferred by this card.

<a id="glm5-checkpoint-resume-h100"></a>
### checkpoint_resume · H100

| Field | Value |
| --- | --- |
| Status | **⚪ unverified** |
| Source pointer | `items.checkpoint_resume.H100` |
| Hardware | `H100` |
| Precision | `bf16` |
| Last verified | `—` |
| Effective Bridge commit | `5db9c262411cf4561d44cdf76a134b423fdaacc2` |

#### Explicit command topology

No launch topology is explicitly encoded in the command.

#### Configuration dimensions

None recorded.

#### Enabled features

None recorded.

#### Available metrics

| Field | Value |
| --- | --- |
| `initial_loss` | `—` |
| `final_loss` | `—` |
| `last_10_steps_step_time_ms_avg` | `—` |
| `last_10_steps_model_tflops_per_gpu_avg` | `—` |
| `last_10_steps_tokens_per_second_per_gpu_avg` | `—` |

#### Exact command

No runnable command is recorded for this status.

#### Expected result

A direct continuation must restore model, optimizer, scheduler, data-order, and RNG state, match declared sentinel losses, and save a reloadable checkpoint to a distinct output root. Training is deferred by this card.
