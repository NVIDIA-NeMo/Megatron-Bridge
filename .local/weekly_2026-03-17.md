# Weekly Maintenance Report — 2026-03-17 (Monday)

Mode: **PR Review**

---

## Follow-Up Tracker

### needs-follow-up (Updated)

Items where the author or a new contributor has responded since the label was applied.

| # | Type | Title | Author | Last Update | Summary of Update |
|---|---|---|---|---|---|
| [#2657](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2657) | Issue | Suggestion: add compact troubleshooting guide | @onestardao | 2026-03-10 | Author offered to write a user-perspective docs draft (6 failure families with symptom/cause/fix structure); maintainer accepted. |
| [#1983](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/1983) | Issue | Checkpoint save crashes in ModelOpt plugin | @TedLiangHK | 2026-03-17 | New user @berserkr confirms hitting the same bug today. Strong signal this is still live. |
| [#2655](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2655) | Issue | Exporting Megatron checkpoint to HF fails | @jaeminh | 2026-03-13 | Author acknowledged the fix PR and said they'd review it. |
| [#2342](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2342) | PR | support qwen3-omni-moe | @zhang-cheng09 | 2026-03-13 | PR is APPROVED but CI failures remain (Installation Test, Unit Tests); needs CI re-trigger and fix. |
| [#2644](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2644) | PR | Store hf_pretrained as properties of Megatron*Bridge classes | @HollowMan6 | 2026-03-16 | Activity today (likely CI); still has needs-author + needs-follow-up. |
| [#2469](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2469) | PR | Add GLM5 support | @pengdurice | 2026-03-12 | Author pushed commits since label was applied (CI ran Mar 12). |

**Action needed:** Re-review #2657 (invite docs PR), escalate #1983 (second user confirmed), check #2342 CI re-trigger.

### needs-follow-up (Stale)

Items with no activity since the label was applied.

| # | Type | Title | Author | Days Stale | Notes |
|---|---|---|---|---|---|
| [#2763](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2763) | Issue | Support Megatron-FSDP MixedPrecisionPolicy dtype args | @yaoyu-33 | 6 | Maintainer's own issue; no comments. Tracking feature work. |
| [#2725](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2725) | Issue | DeepEP rejects B200/B300 with variant name | @arianmag | 7 | Fix PR #2731 opened but CI failing — label will resolve once PR merges. |
| [#2674](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2674) | Issue | How to parse nargs='+' args | @cryoco | 7 | Support question answered by maintainer on Mar 10; no author reply. Consider closing. |
| [#2667](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2667) | Issue | cp_comm_type=a2a+p2p silently disables CP | @shanecmoran | 12 | Fix is draft PR #2665; still waiting. |
| [#2652](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2652) | Issue | Add Comet ML tracking | @shanecmoran | 7 | PR #2653 open and under review. |
| [#2640](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2640) | Issue | MoE checkpoint export fails (YaRN + flex) | @shanecmoran | 7 | Fix in PR #2641; no CI run yet. |
| [#2391](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2391) | Issue | Support num_epochs in TrainingConfig | @eladg9 | 27 | Maintainers asked for dataset details on Feb 18; no response. Consider closing. |
| [#1986](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/1986) | Issue | Multiple LoRA adapters support | @xvrong | 55+ | On roadmap, assigned yaoyu-33. Low priority. |
| [#1536](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/1536) | Issue | Save only LoRA weights | @wuyaoxuehun | 100+ | On roadmap since Dec 2025, no update. |
| [#2653](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2653) | PR | Add Comet ML experiment tracking | @shanecmoran | 13 | No reviewer assigned; community PR waiting. |
| [#2522](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2522) | PR | Auto-slice output vocab for SFT answer_only_loss | @AmitMY | 20 | DCO `ACTION_REQUIRED` — merge blocked until author signs commits. |
| [#2470](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2470) | PR | Add on_before_data_init callback (MLPerf) | @matthew-frank | 25 | No review; community PR waiting 25 days. |
| [#1481](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/1481) | Issue | Convert HF weights to Megatron failed | @Giyu917 | — | **RESOLVED** — author confirmed on 2025-12-10. Remove needs-follow-up label. |
| [#1624](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/1624) | PR | Log test loss separately from val loss | @ezKoya | 60+ | DCO `ACTION_REQUIRED`, last activity Jan 2026. |
| [#1209](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/1209) | PR | Optional kwargs+train_state in dataset providers | @pilot7747 | 26+ | APPROVED but L2 training + L2 recipe CI failures from Feb 19. Needs CI re-run. |
| [#1462](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/1462) | PR | Add Falcon H1 | @dhiaEddineRhaiem | 28+ | APPROVED, lint FAILURE + DCO `ACTION_REQUIRED`. Merge blocked. |

### needs-author (Updated)

Author has responded — ready for maintainer re-engagement.

| # | Type | Title | Author | Last Update | Summary of Update |
|---|---|---|---|---|---|
| [#2785](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2785) | Issue | Torch FSDP2 does not work | @pavelgein | 2026-03-14 | Author confirmed use case (SFT Qwen3-8B, 16k seq, packed); matches documented FSDP recommendation. Maintainer posted detailed 3-bug root-cause table. |
| [#2701](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2701) | Issue | qwen35 0.8B ValueError: too many values to unpack | @li126com | 2026-03-11 | Author confirmed transformers 5.2.0 required; maintainer noted latest branch has Megatron-native vision model. |
| [#2684](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2684) | Issue | ValueError: CPU Offloading is enabled | @santurini | 2026-03-11 | Author provided full repro config + container (nemo:26.02.00, v0.3.0) and added second error (cuda_graph full_iteration + transformer_engine impl). |

**Action needed:** Review #2785 fix progress (3 bugs — Bugs 1+2 verified, Bug 3 pending), re-engage #2701 and #2684 with fix status.

### needs-author (Stale)

No author response yet.

| # | Type | Title | Author | Days Stale | Notes |
|---|---|---|---|---|---|
| [#2717](https://github.com/NVIDIA-NeMo/Megatron-Bridge/issues/2717) | Issue | attention mask should be bool type | @haiasd | 7 | Maintainer asked for repro + stack trace on Mar 10; no response. |

---

## PR Review (Monday — ordered P0 > P1 > P2 > P3, oldest first within each tier)

---

### P0 — Urgent

#### #2742 — [sync][training] fix: backward compat for hybrid_override_pattern in _load_args_from_checkpoint

| Field | Value |
|---|---|
| Author | `mehraakash` |
| Type | `bug` |
| Area | `area:training` |
| Labels | `bug`, `ready-to-merge`, `area:training` |
| Label Changes | None needed |
| CI / Merge | APPROVED · Nemo_CICD_Test SUCCESS · codecov/patch FAILURE (non-blocking) |
| Created | 2026-03-11 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2742 |

**Summary:** Backward-compat fix for `hybrid_override_pattern` field in checkpoint arg loading. APPROVED with all functional CI green; only codecov/patch is failing (coverage threshold for the patch, not a functional blocker).

**Findings:** No critical/major findings. codecov/patch failures are cosmetic for a backcompat fix. **Safe to merge.**

---

#### #2741 — [sync][training] fix: free overlap buffers and empty cache before checkpoint save

| Field | Value |
|---|---|
| Author | `mehraakash` |
| Type | `bug` |
| Area | `area:training` |
| Labels | `ready-to-merge` |
| Label Changes | Add `bug`, `area:training` |
| CI / Merge | APPROVED · Launch_Unit_Tests FAILURE · Nemo_CICD_Test FAILURE |
| Created | 2026-03-11 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2741 |

**Summary:** Fix to free overlap communication buffers and empty CUDA cache before checkpoint save to prevent OOM during saves in DDP overlap scenarios.

**Findings:** No critical/major findings. CI failures need to be fixed and retried to land.

---

#### #2731 — [training] fix: use startswith for Blackwell GPU name validation in DeepEP

| Field | Value |
|---|---|
| Author | `yaoyu-33` |
| Type | `bug` |
| Area | `area:training` |
| Labels | `ready-to-merge`, `area:training` |
| Label Changes | Add `bug` |
| CI / Merge | APPROVED · Launch_Unit_Tests FAILURE · Nemo_CICD_Test FAILURE |
| Created | 2026-03-10 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2731 |

**Summary:** Fixes DeepEP validation that rejects Blackwell GPU naming variants (e.g. `"NVIDIA B300 SXM6 AC"`) by switching the exact-match check to `startswith`.

**Findings:** No critical/major findings. CI failures (likely flaky or pre-existing) need to be investigated and resolved to land.

---

#### #2802 — cp: Onboard NVFP4 and MXFP8 recipes (2600) into r0.3.0

| Field | Value |
|---|---|
| Author | `svcnvidia-nemo-ci` (bot) |
| Type | `ci` |
| Area | `area:perf` (recipe perf) |
| Labels | `Run CICD`, `cherry-pick`, `area:perf`, `needs-review` |
| Label Changes | None needed |
| CI / Merge | REVIEW_REQUIRED · CI all green |
| Created | 2026-03-14 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2802 |

**Summary:** Cherry-picks NVFP4/MXFP8 recipe variants (DeepSeek-V3, Qwen3-235B, GPT-OSS 120B) into the r0.3.0 release branch; wires `apply_flex_dispatcher_backend` into GPT-OSS hardware configs.

**Findings:**
- NVFP4 configs are defined as aliases of BF16 base configs — precision settings are applied at runtime via recipe-name lookup. Confirm the new NVFP4 names are covered by the harness recipe-to-precision mapping end-to-end before merging to release.
- Otherwise no critical findings. CI is green. Needs explicit reviewer approval before merge.

---

### P1 — High

#### #2825 — [training, fix] Support packed sequence packing with pipeline parallelism

| Field | Value |
|---|---|
| Author | `cuichenx` |
| Type | `bug` |
| Area | `area:training` |
| Labels | `bug`, `area:training`, `needs-review` |
| Label Changes | None needed |
| CI / Merge | APPROVED (DCO+secrets only) · Full CICD suite NOT RUN |
| Created | 2026-03-16 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2825 |

**Summary:** Fixes crash when `pack_sequences_in_batch=True` + PP>1: non-last PP stages don't receive labels/loss_mask, so `pack_batch_sequences` now accepts `None` for those tensors; `runtime_config_update` silently sets `variable_seq_lengths=True` post-validate to avoid false-positive MoE dispatcher check.

**Findings:**
- **Major: Full CICD suite has not run.** Only DCO and secrets checks passed. This is a bug fix in active training paths (config.py + vlm_step.py); merging without full unit + L2 CI is risky. Trigger CI before merge.

---

#### #2799 — Fix Megatron→HF export when PP>1 for Qwen3.5 VL MoE models with MTP enabled

| Field | Value |
|---|---|
| Author | `HollowMan6` (community) |
| Type | `bug` |
| Area | `area:ckpt` |
| Labels | `bug`, `community-request`, `area:ckpt` |
| Label Changes | None needed |
| CI / Merge | REVIEW_REQUIRED · Launch_Unit_Tests FAILURE · Nemo_CICD_Test FAILURE |
| Created | 2026-03-14 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2799 |

**Summary:** When PP>1 + MTP enabled, the last PP stage holds duplicate word embedding weights used by MTP. This PR skips those duplicates during HF export via `_should_skip_mtp_duplicate_embedding_export`, preventing corruption. 247 lines of unit tests added.

**Findings:**
- **Major: CI failures present.** `Launch_Unit_Tests` and `Nemo_CICD_Test` FAILURE. Needs investigation before merge.
- The skip logic relies implicitly on `mtp_process` being set only on the final PP stage rather than an explicit PP-rank guard — acceptable as-is but fragile. Document the coupling.
- Trigger `/ok to test <sha>` to re-run CI.

---

#### #2798 — Integrate ModelConfig & ModelBuilder interface into training loop

| Field | Value |
|---|---|
| Author | `maanug-nv` |
| Type | `feature` |
| Area | `area:training` |
| Labels | `feature`, `area:training`, `needs-review` |
| Label Changes | None needed |
| CI / Merge | REVIEW_REQUIRED · CI all green |
| Created | 2026-03-14 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2798 |

**Summary:** Extends the training loop to accept the new `ModelConfig`/`ModelBuilder` interface as an alternative to `ModelProviderMixin` (deprecated with warning). `Serializable` protocol gains `@runtime_checkable`.

**Findings:**
- **Major: Latent runtime crash for fine-tuning paths.** In `train.py`, `getattr(model_config, "seq_length", cfg.model.seq_length)` is called 4× as a fallback. When `cfg.model` is `ModelConfig` (not `ModelProviderMixin`), `cfg.model.seq_length` will raise `AttributeError` since `ModelConfig` doesn't expose `seq_length`. Needs a safe fallback.

---

#### #2715 — [ci] refactor: tier functional tests into L0/L1/L2

| Field | Value |
|---|---|
| Author | `yaoyu-33` |
| Type | `ci` |
| Area | `area:misc` |
| Labels | `area:misc`, `needs-review`, `needs-more-tests` |
| Label Changes | Add `ci` type label |
| CI / Merge | REVIEW_REQUIRED · Nemo_CICD_Test FAILURE (`L1_Launch_quantization_export` failing) |
| Created | 2026-03-10 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2715 |

**Summary:** Splits functional CI into L0 (per-PR), L1 (main push + `needs-more-tests` label), and L2 (nightly), dramatically reducing per-PR CI time.

**Findings:**
- **Major: `L1_Launch_quantization_export` is FAILING.** Cannot merge a CI tier refactor with a broken L1 gate — would immediately break every PR that gets `needs-more-tests`. Must fix the failing job before merging.

---

#### #2685 — [ckpt] refactor: Consolidate fused expert mappings and fix MTP inference

| Field | Value |
|---|---|
| Author | `yaoyu-33` |
| Type | `refactor` |
| Area | `area:ckpt`, `area:model` |
| Labels | _(none)_ |
| Label Changes | Add `feature` (or `ci`), `area:ckpt`, `area:model`, `needs-review` |
| CI / Merge | REVIEW_REQUIRED · Only secrets-detector + DCO ran; full CICD NOT run |
| Created | 2026-03-06 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2685 |

**Summary:** Introduces generic `FusedExpertMapping`/`FusedGatedExpertMapping` classes replacing per-model expert mapping code across 5+ bridges. Also fixes MTP inference by setting `mtp_num_layers=0` and clearing `mtp_process`.

**Findings:**
- **Major: No CICD run.** Large structural change across multiple bridge files with no CI evidence. Must trigger before merge.
- **Major: `ColumnParallelMapping._dtype_warned` is class-level.** Once set `True`, all future dtype mismatches across all instances are silently swallowed for the process lifetime. This can hide real correctness issues in multi-model runs or test suites.
- No unit tests for `FusedExpertMapping`/`FusedGatedExpertMapping` round-trips.

---

#### #2695 — [model] feat: add homogeneous MIMO mode for standard pretrain path

| Field | Value |
|---|---|
| Author | `aroshanghias-nvd` |
| Type | `feature` |
| Area | `area:model`, `area:training` |
| Labels | `area:model`, `needs-review` |
| Label Changes | Add `feature`, `area:training` |
| CI / Merge | REVIEW_REQUIRED · Only secrets-detector + DCO ran; full CICD NOT run |
| Created | 2026-03-08 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2695 |

**Summary:** Extends `MimoModelProvider` for "homogeneous" mode (no `mimo_parallelism_config`) routing through the standard pretrain path via `_MimoConfigProxy` delegation. PP=1 is enforced for this mode.

**Findings:**
- **Major: No CICD run.** Large feature with no CI coverage.
- **Major: Loss mask correctness bug in dataset.py.** `pad_token_id` fallback defaults to `0` when `tokenizer.pad_token_id` is `None`. For tokenizers where index 0 is `<bos>` or `<unk>`, all positions predicting that token are incorrectly zeroed from the loss — silently corrupting training.
- `_MimoConfigProxy.__setattr__` always writes to inner `TransformerConfig`, never to proxy — attributes not on `TransformerConfig` would silently be added there.

---

#### #2641 — [model, training] fix: MoE checkpoint export with YaRN RoPE and flex dispatcher

| Field | Value |
|---|---|
| Author | `shanecmoran` (community) |
| Type | `bug` |
| Area | `area:ckpt`, `area:model` |
| Labels | `community-request` |
| Label Changes | Add `bug`, `area:ckpt`, `area:model`, `needs-review` |
| CI / Merge | REVIEW_REQUIRED · Only DCO ran; no CICD |
| Created | 2026-03-04 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2641 |

**Summary:** Two bug fixes for `Qwen3MoEModelProvider`: adds missing YaRN RoPE fields to prevent silent OmegaConf drops; downgrades `flex` token dispatcher to `alltoall` when TP*EP ≤ 1 during single-GPU export.

**Findings:**
- **Major: No CICD run.** Community PR touching dispatcher fallback at load time — must have green full CI before merge. Trigger `/ok to test <sha>`.
- Dispatcher check correctly reads post-`mp_overrides` values (ordering is safe as written), but the ordering dependency should be documented.

---

#### #2700 — [ckpt] fix: minor quantization export issue

| Field | Value |
|---|---|
| Author | `yueshen2016` |
| Type | `bug` |
| Area | `area:ckpt` |
| Labels | `area:ckpt`, `needs-review` |
| Label Changes | Add `bug` |
| CI / Merge | REVIEW_REQUIRED · Full CICD unknown (no rollup in metadata) |
| Created | 2026-03-08 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2700 |

**Summary:** Minor fix for quantization export path in checkpoint conversion.

**Findings:** No critical/major findings from available metadata. Requires full CI verification.

---

### P2 — Normal

#### #2826 — Unify dataset configuration in run_recipe.py

| Field | Value |
|---|---|
| Author | `cuichenx` |
| Type | `feature` |
| Area | `area:training` |
| Labels | _(none)_ |
| Label Changes | Add `feature`, `area:training`, `needs-review` |
| CI / Merge | APPROVED · CI green |
| Created | 2026-03-17 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2826 |

**Summary:** Replaces `--mode`/`--finetune_dataset`/`--packed_sequence` CLI flags in `run_recipe.py` with a unified `--dataset <type>` flag backed by `apply_dataset_override`; 270 new unit tests. Training mode is now inferred from dataset type.

**Findings:**
- **Major: Unsafe hardcoded default for `--dataset vlm-hf`.** `HFDatasetConversationProvider` is initialized with `maker_name="make_cord_v2_dataset"`. Whether CLI overrides to `dataset.maker_name` actually override this attribute depends on downstream Hydra processing — if not, every `--dataset vlm-hf` run silently uses cord_v2. Verify end-to-end before merge.

---

#### #2780 — added logic for specific checkpointing directory (llama31/perf scripts)

| Field | Value |
|---|---|
| Author | `rsalagame-nvidia` |
| Type | `feature` |
| Area | `area:perf` |
| Labels | `feature`, `area:perf`, `needs-review` |
| Label Changes | None needed |
| CI / Merge | REVIEW_REQUIRED · DCO `ACTION_REQUIRED` |
| Created | 2026-03-12 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2780 |

**Summary:** Adds `--save_dir` to `scripts/performance/setup_experiment.py`; creates the directory and mounts it into the container.

**Findings:**
- **Major: DCO `ACTION_REQUIRED` — merge blocker.** Author has not signed their commits. Cannot merge until resolved.

---

#### #2773 — Announce Python 3.12 migration

| Field | Value |
|---|---|
| Author | `ko3n1g` |
| Type | `docs` |
| Area | `area:build` |
| Labels | `docs-only`, `docs`, `area:build`, `needs-review` |
| Label Changes | None needed |
| CI / Merge | REVIEW_REQUIRED · CI green |
| Created | 2026-03-12 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2773 |

**Summary:** Docs-only PR announcing the upcoming Python 3.12 migration (paired with draft #2774).

**Findings:** No critical/major findings.

---

#### #2736 — LoRA bridge & merge for Qwen3.5

| Field | Value |
|---|---|
| Author | `HollowMan6` (community) |
| Type | `feature` |
| Area | `area:peft` |
| Labels | `community-request`, `area:peft`, `needs-review` |
| Label Changes | Add `feature` |
| CI / Merge | REVIEW_REQUIRED · CI unknown |
| Created | 2026-03-10 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2736 |

**Summary:** Adds LoRA bridge and weight merging support for Qwen3.5 models.

**Findings:** No critical/major findings from available metadata. Needs full review.

---

#### #2630 — [misc] feat: Add bidirectional MLM <-> MBridge config translation script

| Field | Value |
|---|---|
| Author | `yaoyu-33` |
| Type | `feature` |
| Area | `area:misc` |
| Labels | `needs-review` |
| Label Changes | Add `feature`, `area:misc` |
| CI / Merge | REVIEW_REQUIRED · Full CICD green · codecov/patch FAILURE |
| Created | 2026-03-03 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2630 |

**Summary:** Adds 1,635-line `scripts/translate_mlm_to_bridge.py` for bidirectional translation between Megatron-LM CLI args and Bridge Hydra overrides; vanilla_gpt recipe; 957 lines of unit tests.

**Findings:**
- codecov/patch FAILURE: new code below coverage threshold. Verify whether uncovered branches include risk-bearing paths (YAML parsing, `--emit recipe`, reverse-direction error handling).
- The translator is pure-Python with no import of Bridge config types — no static verification that emitted override keys are valid. Large maintenance surface.
- Otherwise no critical/major findings. Full CICD is green.

---

#### #2801 — Delete tests/functional_tests/L2_Mcore_Mock_Tests_GPU.sh

| Field | Value |
|---|---|
| Author | `huvunvidia` |
| Type | `ci` |
| Area | `area:misc` |
| Labels | `ci`, `area:misc` |
| Label Changes | None needed |
| CI / Merge | APPROVED · Launch_Unit_Tests FAILURE · Nemo_CICD_Test FAILURE |
| Created | 2026-03-14 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2801 |

**Summary:** Deletes a shell script that ran diffusion recipe pytest tests.

**Findings:**
- CI failures at approval time — confirm failures are pre-existing/unrelated (not caused by removing this script) before merging.

---

#### #2264 — [data] Add openmathinstruct and gsm8k datasets

| Field | Value |
|---|---|
| Author | `cuichenx` |
| Type | `feature` |
| Area | `area:data` |
| Labels | `area:data`, `needs-review` |
| Label Changes | Add `feature` |
| CI / Merge | REVIEW_REQUIRED |
| Created | 2026-02-06 |
| Link | https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2264 |

**Summary:** Adds openmathinstruct and GSM8K SFT datasets.

**Findings:** No critical/major findings from available metadata. This PR is 39 days old without review.

---

### P3 — Low (Drafts and low-risk items)

The following open draft or low-risk PRs are noted for completeness:

| # | Title | Author | Draft | Notes |
|---|---|---|---|---|
| [#2819](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2819) | mcore checkpointing sync | `dimapihtar` | Yes | Draft feature |
| [#2810](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2810) | Integrate ModelConfig into conversion | `maanug-nv` | Yes | Companion to #2798 |
| [#2809](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2809) | [doc] split technique guidance into docs/skills/knowledge | `yaoyu-33` | Yes | docs-only draft |
| [#2803](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2803) | [recipe][perf] unified flat performance recipes | `yaoyu-33` | Yes | Draft |
| [#2782](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2782) | [data] Generic Energon task encoder for HF-encoder VLMs | `cuichenx` | Yes | Draft |
| [#2774](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2774) | chore: Move to Py3.12 | `ko3n1g` | Yes | Paired with #2773 |
| [#2772](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2772) | NT super perf recipe | `malay-nagda` | Yes | Draft |
| [#2814](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2814) | chore: Bump uv.lock (r0.3.0) | `svcnvidia-nemo-ci` | No | Bot PR; add `ci`, `area:build` |
| [#2812](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2812) | chore: Bump uv.lock (main) | `svcnvidia-nemo-ci` | No | Bot PR; add `ci`, `area:build` |
| [#2528](https://github.com/NVIDIA-NeMo/Megatron-Bridge/pull/2528) | doc - Add SFT dataset formats documentation | `onel` (community) | No | docs-only |

---

## Recap

- **Total PRs reviewed:** 23 (non-draft) + 10 (draft, P3 summary)
- **P0 (Urgent):** 4 — #2742 (safe to merge), #2741 (rtm label wrong, CI failing), #2731 (rtm label wrong, CI failing), #2802 (needs approval)
- **P1 (High):** 9 — #2825, #2799, #2798, #2715, #2685, #2695, #2641, #2700, plus follow-ups on CI
- **P2 (Normal):** 8
- **P3 (Low):** 10+ drafts/chores
- **Follow-up items with updates:** 6 (issues: #2657, #1983, #2655; PRs: #2342, #2644, #2469)
- **Stale needs-author items (> 7 days):** 1 issue (#2717)
- **Label changes recommended:** 15+ PRs missing type or area labels

### Key Observations

- **Two `ready-to-merge` PRs stuck on CI failures.** #2731 and #2741 are APPROVED and conceptually good but `Launch_Unit_Tests` and `Nemo_CICD_Test` are failing — likely flaky or pre-existing. Need investigation and re-trigger to land. The CI tier refactor in #2715 is the right long-term fix.
- **Large batch of community-request PRs with needs-author.** 15+ community PRs are in `needs-author` state — many updated today from label/CI activity rather than actual author responses. The backlog is growing.
- **#1983 re-surfaced today.** A second user hit the ModelOpt `ProcessGroupsConfig` checkpoint crash. Worth re-prioritizing this fix.
- **#2695 has a loss computation bug.** The `pad_token_id=0` fallback in the MIMO dataset silently masks tokens at index 0 from the loss. Should be flagged to the author before merge.
- **Most unlabeled PRs are internal NVIDIA author PRs** — the labeling gap is concentrated in older internal PRs that predate the taxonomy.

---

## TODO

- [ ] **Merge #2742** — APPROVED + CI green (only codecov/patch minor failure). Safe to merge now.
- [ ] **Fix CI on #2741 and #2731** — investigate `Launch_Unit_Tests` + `Nemo_CICD_Test` failures and re-trigger until green; both are APPROVED and ready to land once CI passes.
- [ ] **Approve and merge #2802** — cherry-pick to r0.3.0, CI green, needs reviewer approval; confirm NVFP4 recipe-name mapping end-to-end.
- [ ] **Fix `L1_Launch_quantization_export` failure in #2715** — CI tier refactor cannot land with a broken L1 gate.
- [ ] **Trigger CI on #2641** — community bug fix, no CICD run; `/ok to test <sha>`.
- [ ] **Trigger CI on #2799** — community bug fix with CI failures; investigate or re-trigger.
- [ ] **Fix #2798 latent crash** — `cfg.model.seq_length` fallback in `train.py` will `AttributeError` for `ModelConfig` users in fine-tuning paths.
- [ ] **Fix #2695 loss mask bug** — `pad_token_id` defaulting to `0` silently corrupts loss for tokenizers with `<bos>`/`<unk>` at index 0. Block merge until fixed.
- [ ] **Fix #2685 `_dtype_warned` class-level flag** — suppresses all future dtype warnings globally after first trigger; replace with instance-level flag.
- [ ] **Remove `needs-follow-up` from #1481** — issue is resolved (author confirmed 2025-12-10).
- [ ] **Close #2674 or ping author** — support question answered Mar 10, no response in 7 days.
- [ ] **Re-review #2657** — invite @onestardao to submit the docs PR draft they offered.
- [ ] **Escalate #1983** — second user hit this today; ModelOpt checkpoint crash is still live in v0.3.0.
- [ ] **DCO blockers: #2780, #2522, #1624, #1462** — ping authors to sign commits (`git commit --amend --signoff`).
- [ ] **Apply missing labels** — #2826 (`feature`, `area:training`), #2685 (`feature`/`refactor`, `area:ckpt`, `area:model`), #2641 (`bug`, `area:ckpt`, `area:model`), #2812/#2814 (`ci`, `area:build`), and others noted above.
